# Disaster_tweets_classifier
Text classifier to classify if a tweet is related to a disaster or not

## version1 - preprocessing + models
## version2 - word embeddings + models
## version3 - word embeddings + neural network
